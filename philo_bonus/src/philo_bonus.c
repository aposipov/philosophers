//
// Created by user on 22.05.22.
//

#include "../inc/philo_bonus.h"

#include "../inc/philo_bonus.h"

int	main(int argc, char **argv)
{
	t_philo	*lunch;

	if (check_arg(argc, argv))
		return (1);
	//all = init_input(argc, argv);

	//if (init_sem_pid(lunch))
	//	return (1);
	//if (start_lunch(lunch))
	//	return (1);
	//all_free(lunch);

	return (0);
}